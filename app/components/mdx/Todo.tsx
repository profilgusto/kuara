/**
 * Todo — MDX block for author TODO notes. Never rendered in the output.
 *
 * Usage in MDX:
 *   <Todo>
 *     Add a diagram for projectile motion here.
 *   </Todo>
 *
 * TODOs can be reviewed globally in the Payload admin at /admin/todos.
 * Delete the block once the item has been addressed.
 */
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export default function Todo(_: { children?: React.ReactNode }) {
  return null;
}
